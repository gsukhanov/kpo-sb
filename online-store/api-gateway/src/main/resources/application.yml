server:
  port: ${SERVER_PORT:8080}

services:
  orders:
    base-url: ${ORDERS_SERVICE_URL:http://orders-service:8081}
  payments:
    base-url: ${PAYMENTS_SERVICE_URL:http://payments-service:8082}

springdoc:
  swagger-ui:
    path: /swagger
  api-docs:
    path: /api-docs
